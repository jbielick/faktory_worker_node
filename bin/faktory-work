#!/usr/bin/env node

const faktory = require("../lib/faktory");
console.log(faktory);
process.exit(0);
const program = require("../lib/cli");

faktory.work(program).catch((error) => {
  console.error(error);
  process.exit(1);
});
